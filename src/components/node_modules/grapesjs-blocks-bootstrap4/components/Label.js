export default (dc, traits, config = {}) => {

    const textType = dc.getType('text');
    const textModel = textType.model;
    const textView = textType.view;

    dc.addType('bs_label', {
        model: textModel.extend({
            defaults: {
                ...textModel.prototype.defaults,
                'custom-name': config.labels.label,
                tagName: 'label',
                traits: [traits.for],
            },
        }, {
            isComponent(el) {
                if(el.tagName == 'LABEL' && el.classList && el.classList.contains('bs-label')){
                    return {type: 'bs_label'};
                }
            },
        }),
        view: textView,
    });
}
